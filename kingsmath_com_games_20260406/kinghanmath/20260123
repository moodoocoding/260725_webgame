<!DOCTYPE html>
<html lang="en">
<head>
<script async src="https://www.googletagmanager.com/gtag/js?id=G-5YW0T2C109"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());

  gtag('config', 'G-5YW0T2C109');
</script>
    <meta charset="UTF-8">
    <meta name="description" content="Master arithmetic with King's Math Battle! Free math training game for smart boards and large screens.">
    <meta name="keywords" content="Math Game, KingsMath, Mental Math, Educational Game, Free Game, Arithmetic, Multiplication, Brain Training">
    <link rel="canonical" href="http://kingsmath.com">

    <meta property="og:type" content="website">
    <meta property="og:title" content="King's Math Battle | KingsMath.com">
    <meta property="og:description" content="Free arithmetic battle game optimized for smart boards.">
    <meta property="og:image" content="http://11ukul.com/wp-content/uploads/2026/01/한메-킹수학-교사_킹수학-전자칠판-전용-수학-배틀-게임.png">
    <meta property="og:url" content="http://kingsmath.com">
    <meta property="og:site_name" content="KingsMath">

    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="King's Math Battle | KingsMath.com">
    <meta name="twitter:description" content="Free arithmetic battle game optimized for smart boards.">
    <meta name="twitter:image" content="http://11ukul.com/wp-content/uploads/2026/01/한메-킹수학-교사_킹수학-전자칠판-전용-수학-배틀-게임.png">
    <meta name="theme-color" content="#0F4C3A">

    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>King's Math Battle</title>
    <style>
        @font-face {
            font-family: 'DosMyungjo';
            src: url('https://cdn.jsdelivr.net/gh/projectnoonnu/noonfonts_eight@1.0/DOSMyungjo.woff') format('woff');
            font-weight: normal;
            font-display: swap;
        }

        :root {
            --win-bg: #208585;
            --dos-blue: #0000A8;
            --dos-gray: #C0C0C0;
            --key-purple: #d6bcfa;
        }

        body, html {
            margin: 0; padding: 0;
            width: 100%; height: 100%;
            background-color: #000;
            /* 다국어 지원을 위한 폰트 폴백 추가 */
            font-family: 'DosMyungjo', 'Courier New', Courier, monospace, sans-serif; 
            overflow: hidden;
            touch-action: none; 
        }

        #app-container {
            width: 800px; 
            height: 600px;
            display: flex;
            flex-direction: column;
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            background-color: #000;
            box-shadow: 0 0 0 2px #333;
            transform-origin: center center;
            z-index: 10;
        }

        /* --- 상단 바 --- */
        #top-bar {
            height: 32px;
            background-color: var(--dos-blue);
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0 10px;
            color: white;
            font-size: 20px;
            box-sizing: border-box;
            z-index: 50;
        }

        .deco-box {
            width: 16px; 
            height: 16px; 
            background-color: #fff;
            display: inline-block;
        }

        #title-text {
            letter-spacing: 2px;
            font-weight: bold;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        /* --- 게임 영역 --- */
        #game-viewport {
            flex: 1;
            position: relative;
            background-color: var(--win-bg);
            overflow: hidden;
        }

        #info-bar {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 80px;
            background-color: transparent;
            color: black;
            display: flex;
            flex-direction: column;
            justify-content: flex-start; 
            align-items: center;
            font-size: 24px;
            z-index: 60;
            pointer-events: none;
            padding-top: 10px;
        }

        #ui-score-container {
            font-weight: bold;
            text-shadow: 1px 1px 0 rgba(255,255,255,0.5);
        }

        /* 아이템 메시지 */
        #item-message {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            color: #ffe200;
            font-size: 40px; 
            font-weight: bold;
            text-shadow: 3px 3px 0 #000;
            width: 100%;
            text-align: center;
            opacity: 0;
            transition: opacity 0.3s;
            z-index: 80; 
            pointer-events: none;
        }

        #clock-container {
            position: absolute;
            right: 0px;
            top: 0px;
            width: 100px;
            height: 100px;
            background-color: #ffe200;
            box-sizing: border-box;
            pointer-events: auto;
        }

        canvas#analog-clock {
            width: 100%;
            height: 100%;
            display: block;
        }

        canvas#gameCanvas { display: block; width: 100%; height: 100%; }

        /* 컨트롤 패널 */
        #control-panel {
            position: absolute;
            bottom: 0px;
            left: 50%;
            transform: translateX(-50%);
            display: flex;
            flex-direction: column;
            align-items: center;
            z-index: 20;
        }

        #lcd-screen {
            width: 197px;
            height: 73px;
            background-color: #eee;
            border: 2px solid #555;
            margin-bottom: -1px;
            display: flex;
            justify-content: center;
            align-items: center;
            color: rgba(0,0,0,0.9);
            font-size: 32px;
            letter-spacing: 2px;
            box-shadow: inset 0 0 10px rgba(0,255,0,0.2);
        }

        #keypad {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 0px;
            width: 180px;
        }

        .key {
            height: 46px;
            background-color: var(--key-purple);
            border: 2px solid rgba(107, 70, 193, 0.8);
            display: flex;
            justify-content: center;
            align-items: center;
            font-size: 22px;
            color: #2d1b4d;
            cursor: pointer;
            user-select: none;
            box-shadow: 0 3px 0 #553c9a;
            transition: transform 0.05s;
        }

        .key:active { transform: translateY(2px); box-shadow: 0 1px 0 #553c9a; }
        .key-enter { background-color: #f687b3; box-shadow: 0 3px 0 #b83280; color: white; }
        .key-clear { background-color: #63b3ed; box-shadow: 0 3px 0 #2b6cb0; color: white; }

        .key.broken {
            background-color: #2d1b4d !important;
            color: #7c6ba3 !important;
            box-shadow: none !important;
            background-image: repeating-linear-gradient(45deg, #000 0, #000 1px, transparent 1px, transparent 4px);
            transform: translateY(2px);
        }

        /* --- 하단 바 --- */
        #bottom-bar {
            height: 36px;
            background-color: var(--dos-gray);
            border-top: 2px solid #fff;
            display: flex;
            align-items: stretch;
            font-size: 16px;
            color: #000;
            box-sizing: border-box;
            z-index: 50;
        }

        #bottom-left {
            flex: 2;
            display: flex;
            align-items: center;
            padding-left: 15px;
            border-right: 2px solid #888;
            white-space: nowrap; overflow: hidden; text-overflow: ellipsis;
        }

        #bottom-right {
            flex: 1;
            display: flex;
            align-items: center;
            justify-content: flex-end;
            padding-right: 20px;
            white-space: nowrap; overflow: hidden; text-overflow: ellipsis;
        }

        /* 오버레이 */
        .overlay-screen {
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            background-color: rgba(0,0,0,0.85);
            z-index: 100;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            color: #fff;
            text-align: center;
        }
        
        .overlay-screen h2 {
            font-size: 40px; 
            margin: 0 0 30px 0;
            text-shadow: 4px 4px 0 #000;
            color: #ff5555;
            padding: 0 20px;
            word-break: keep-all;
        }

        .overlay-screen p { 
            font-size: 24px; 
            margin: 20px 0; 
            padding: 0 20px;
            word-break: keep-all;
        }

        .overlay-btn {
            padding: 15px 40px;
            font-family: inherit;
            font-size: 24px;
            background-color: #0000A8;
            color: #fff;
            border: 4px solid #fff;
            cursor: pointer;
            box-shadow: 6px 6px 0 #000;
        }
        .overlay-btn:active {
            transform: translate(4px, 4px);
            box-shadow: 2px 2px 0 #000;
        }

        /* 다른 게임 고르기 링크 스타일 */
        .other-game-link {
            display: block;
            margin-top: 25px;
            font-size: 18px;
            color: #aaa;
            text-decoration: none;
            cursor: pointer;
        }
        .other-game-link:hover {
            color: #fff;
            text-decoration: underline;
        }
        
        #start-screen { display: flex; }
        #game-over-screen { display: none; }

        /* 플로팅 버튼 스타일 */
        .floating-btn {
            position: fixed; 
            bottom: 20px;   
            width: 44px;
            height: 44px;
            background-color: rgba(255, 255, 255, 0.8);
            border: 2px solid #000;
            border-radius: 8px;
            font-size: 24px;
            display: flex;
            justify-content: center;
            align-items: center;
            cursor: pointer;
            z-index: 9999; 
            box-shadow: 2px 2px 0 #000;
        }
        .floating-btn:active {
            transform: translate(2px, 2px);
            box-shadow: none;
        }

        #btn-home { left: 20px; }  
        #btn-fs { right: 20px; }   

        /* 언어 선택바 스타일 (신규 추가) */
        .lang-switcher {
            position: fixed;
            top: 20px;
            right: 20px;
            background: #aaaaaa52;
            border-radius: 8px;
            padding: 8px 15px;
            display: flex;
            gap: 12px;
            box-shadow: 2px 2px 5px rgba(0,0,0,0.5);
            z-index: 10000;
            white-space: nowrap;
        }

        .lang-btn {
            cursor: pointer;
            font-size: 24px;
            text-decoration: none;
            border: none;
            background: none;
            padding: 0;
            transition: transform 0.1s;
            line-height: 1;
        }
        .lang-btn:hover { transform: scale(1.2); }
        .lang-btn:active { transform: scale(0.9); }

    </style>
</head>
<body>

<div class="lang-switcher">
    <button class="lang-btn" onclick="setLanguage('ko')" title="한국어">🇰🇷</button>
    <button class="lang-btn" onclick="setLanguage('en')" title="English">🇺🇸</button>
    <button class="lang-btn" onclick="setLanguage('zh')" title="中文">🇨🇳</button>
    <button class="lang-btn" onclick="setLanguage('ja')" title="日本語">🇯🇵</button>
    <button class="lang-btn" onclick="setLanguage('es')" title="Español">🇪🇸</button>
    <button class="lang-btn" onclick="setLanguage('fr')" title="Français">🇫🇷</button>
    <button class="lang-btn" onclick="setLanguage('ru')" title="Русский">🇷🇺</button>
</div>

<div id="app-container">
    <div id="top-bar">
        <div class="deco-box"></div>
        <div id="title-text" data-i18n="gameTitle">King's Math Battle</div>
        <div class="deco-box"></div>
    </div>

    <div id="game-viewport">
        <div id="item-message"></div>

        <div id="info-bar">
            <span id="ui-score-container"><span data-i18n="scorePrefix">Score : </span> <span id="ui-score">0</span></span>
            
            <div id="clock-container">
                <canvas id="analog-clock" width="40" height="40"></canvas>
            </div>
        </div>

        <canvas id="gameCanvas"></canvas>

        <div id="control-panel">
            <div id="lcd-screen"></div>
            <div id="keypad">
                <div class="key" onclick="keyInput('1')">1</div>
                <div class="key" onclick="keyInput('2')">2</div>
                <div class="key" onclick="keyInput('3')">3</div>
                <div class="key" onclick="keyInput('4')">4</div>
                <div class="key" onclick="keyInput('5')">5</div>
                <div class="key" onclick="keyInput('6')">6</div>
                <div class="key" onclick="keyInput('7')">7</div>
                <div class="key" onclick="keyInput('8')">8</div>
                <div class="key" onclick="keyInput('9')">9</div>
                <div class="key key-clear" onclick="keyInput('C')">C</div>
                <div class="key" onclick="keyInput('0')">0</div>
                <div class="key key-enter" onclick="keyInput('E')">↵</div>
            </div>
        </div>

        <div id="start-screen" class="overlay-screen">
            <h2 data-i18n="startScreenTitle">Arithmetic Venice</h2>
            <p data-i18n="startScreenDesc">Use keyboard or touch pad to answer!</p>
            <button class="overlay-btn" onclick="initGame()" data-i18n="startBtn">START GAME</button>
            <a href="http://kingsmath.com" class="other-game-link" id="link-other-game" data-i18n="otherGameLink">🎮 More Games</a>
        </div>

        <div id="game-over-screen" class="overlay-screen">
            <h2 data-i18n="gameOverTitle">GAME OVER</h2>
            <p><span data-i18n="finalScorePrefix">Final Score: </span> <span id="final-score">0</span></p>
            <button class="overlay-btn" onclick="initGame()" data-i18n="restartBtn">RETRY</button>
            <a href="http://kingsmath.com" class="other-game-link" id="link-other-game-over" data-i18n="otherGameLink">🎮 More Games</a>
        </div>
    </div>

    <div id="bottom-bar">
        <div id="bottom-left" data-i18n="footerLeft">Large screen optimized.</div>
        <div id="bottom-right" data-i18n="footerRight">KingsMath.com</div>
    </div>
</div>

<div id="btn-home" class="floating-btn" onclick="goToHome()" title="Home">🏠</div>
<div id="btn-fs" class="floating-btn" onclick="toggleFullScreen()" title="Full Screen">⛶</div>

<script>
    /* -------------------------------------------------------------------------- */
    /* i18n Logic                                  */
    /* -------------------------------------------------------------------------- */
    const translations = {
        ko: {
            pageTitle: "한메킹수학교사 | 킹수학.com",
            metaDesc: "킹수학으로 전자칠판을 최고의 수학 학습 도구로 만드세요! 무료 수학 배틀 게임.",
            gameTitle: "한 메 킹 수 학 교 사",
            scorePrefix: "점 수 : ",
            startScreenTitle: "연산 베네치아",
            startScreenDesc: "키보드나 터치패드로 정답을 넣으세요!",
            startBtn: "게임 시작",
            otherGameLink: "🎮 다른 게임 고르기",
            gameOverTitle: "게임 종료",
            finalScorePrefix: "최종 점수: ",
            restartBtn: "다시 시작",
            footerLeft: "대화면 전용입니다. 소형 통신 기기는 사용이 불가합니다.",
            footerRight: "정보화 시대의 동반자 킹수학.com",
            linkDomain: "/",
            items: {
                clearAll: "싹쓸이",
                virus: "숨바꼭질 바이러스",
                speedUp: "속도 증가",
                speedDown: "속도 감소",
                heal: "체력 회복",
                freeze: "시간 멈춤"
            }
        },
        en: {
            pageTitle: "King's Math Battle | KingsMath.com",
            metaDesc: "Master arithmetic with King's Math Battle! Free math training game for smart boards.",
            gameTitle: "KING'S MATH BATTLE",
            scorePrefix: "Score : ",
            startScreenTitle: "Math Venice",
            startScreenDesc: "Enter the answer using keypad!",
            startBtn: "START GAME",
            otherGameLink: "🎮 More Games",
            gameOverTitle: "GAME OVER",
            finalScorePrefix: "Final Score: ",
            restartBtn: "RETRY",
            footerLeft: "Optimized for large screens.",
            footerRight: "Partner of Digital Age KingsMath.com",
            linkDomain: "http://kingsmath.com",
            items: {
                clearAll: "Clear All",
                virus: "Virus",
                speedUp: "Speed Up",
                speedDown: "Speed Down",
                heal: "Heal",
                freeze: "Freeze"
            }
        },
        zh: {
            pageTitle: "数学之王挑战 | KingsMath.com",
            metaDesc: "通过数学之王挑战掌握算术！电子白板专用的免费数学训练游戏。",
            gameTitle: "数 学 之 王 挑 战",
            scorePrefix: "分数 : ",
            startScreenTitle: "计算威尼斯",
            startScreenDesc: "请使用键盘输入答案！",
            startBtn: "开始游戏",
            otherGameLink: "🎮 更多游戏",
            gameOverTitle: "游戏结束",
            finalScorePrefix: "最终分数: ",
            restartBtn: "重试",
            footerLeft: "专为大屏幕优化。",
            footerRight: "KingsMath.com",
            linkDomain: "http://kingsmath.com",
            items: {
                clearAll: "全屏清除",
                virus: "病毒",
                speedUp: "加速",
                speedDown: "减速",
                heal: "治疗",
                freeze: "冻结"
            }
        },
        ja: {
            pageTitle: "キング・オブ・算数 | KingsMath.com",
            metaDesc: "キング・オブ・算数で計算をマスターしよう！電子黒板に最適な無料算数ゲーム。",
            gameTitle: "キング・オブ・算数",
            scorePrefix: "点数 : ",
            startScreenTitle: "計算ベネチア",
            startScreenDesc: "キーパッドで答えを入力してください！",
            startBtn: "ゲーム開始",
            otherGameLink: "🎮 他のゲーム",
            gameOverTitle: "ゲームオーバー",
            finalScorePrefix: "最終スコア: ",
            restartBtn: "もう一度",
            footerLeft: "大画面向けに最適化されています。",
            footerRight: "KingsMath.com",
            linkDomain: "http://kingsmath.com",
            items: {
                clearAll: "一掃",
                virus: "ウイルス",
                speedUp: "スピードUP",
                speedDown: "スピードDOWN",
                heal: "回復",
                freeze: "フリーズ"
            }
        },
        es: {
            pageTitle: "Rey de las Matemáticas | KingsMath.com",
            metaDesc: "¡Domina la aritmética con Rey de las Matemáticas! Juego gratuito para pizarras inteligentes.",
            gameTitle: "REY DE LAS MATEMÁTICAS",
            scorePrefix: "Puntos : ",
            startScreenTitle: "Venecia Matemática",
            startScreenDesc: "¡Introduce la respuesta con el teclado!",
            startBtn: "JUGAR",
            otherGameLink: "🎮 Más Juegos",
            gameOverTitle: "FIN DEL JUEGO",
            finalScorePrefix: "Puntuación Final: ",
            restartBtn: "REINTENTAR",
            footerLeft: "Optimizado para pantallas grandes.",
            footerRight: "KingsMath.com",
            linkDomain: "http://kingsmath.com",
            items: {
                clearAll: "Limpiar Todo",
                virus: "Virus",
                speedUp: "Acelerar",
                speedDown: "Frenar",
                heal: "Curar",
                freeze: "Congelar"
            }
        },
        fr: {
            pageTitle: "Le Roi des Maths | KingsMath.com",
            metaDesc: "Maîtrisez l'arithmétique avec Le Roi des Maths ! Jeu gratuit pour écrans interactifs.",
            gameTitle: "LE ROI DES MATHS",
            scorePrefix: "Score : ",
            startScreenTitle: "Venise Calcul",
            startScreenDesc: "Entrez la réponse avec le clavier !",
            startBtn: "JOUER",
            otherGameLink: "🎮 Plus de jeux",
            gameOverTitle: "GAME OVER",
            finalScorePrefix: "Score Final: ",
            restartBtn: "REJOUER",
            footerLeft: "Optimisé pour les grands écrans.",
            footerRight: "KingsMath.com",
            linkDomain: "http://kingsmath.com",
            items: {
                clearAll: "Tout Effacer",
                virus: "Virus",
                speedUp: "Accélérer",
                speedDown: "Ralentir",
                heal: "Soigner",
                freeze: "Geler"
            }
        },
        ru: {
            pageTitle: "Король Математики | KingsMath.com",
            metaDesc: "Освой арифметику с Королем Математики! Бесплатная игра для интерактивных досок.",
            gameTitle: "КОРОЛЬ МАТЕМАТИКИ",
            scorePrefix: "Счет : ",
            startScreenTitle: "Венеция Счета",
            startScreenDesc: "Введите ответ с клавиатуры!",
            startBtn: "ИГРАТЬ",
            otherGameLink: "🎮 Другие игры",
            gameOverTitle: "КОНЕЦ ИГРЫ",
            finalScorePrefix: "Итоговый счет: ",
            restartBtn: "ЗАНОВО",
            footerLeft: "Оптимизировано для больших экранов.",
            footerRight: "KingsMath.com",
            linkDomain: "http://kingsmath.com",
            items: {
                clearAll: "Очистить Всё",
                virus: "Вирус",
                speedUp: "Ускорение",
                speedDown: "Замедление",
                heal: "Лечение",
                freeze: "Заморозка"
            }
        }
    };

    let currentLang = 'en'; // Default

    function detectLanguage() {
        // 1. URL Parameter
        const urlParams = new URLSearchParams(window.location.search);
        if (urlParams.has('lang')) {
            const langParam = urlParams.get('lang');
            if (translations[langParam]) return langParam;
        }

        // 2. Local Storage
        const savedLang = localStorage.getItem('kingsMathLang');
        if (savedLang && translations[savedLang]) return savedLang;

        // 3. Browser Language
        const browserLang = navigator.language.slice(0, 2);
        if (translations[browserLang]) return browserLang;

        // 4. Default
        return 'en';
    }

    function setLanguage(lang) {
        if (!translations[lang]) return;
        
        currentLang = lang;
        localStorage.setItem('kingsMathLang', lang);

        // URL Update (pushState)
        const newUrl = new URL(window.location);
        newUrl.searchParams.set('lang', lang);
        window.history.pushState({}, '', newUrl);

        applyTranslations();
    }

    function applyTranslations() {
        const t = translations[currentLang];
        
        // Document Title & Meta
        document.title = t.pageTitle;
        document.querySelector('meta[name="description"]').setAttribute("content", t.metaDesc);
        document.querySelector('meta[property="og:title"]').setAttribute("content", t.pageTitle);
        document.querySelector('meta[property="og:description"]').setAttribute("content", t.metaDesc);

        // UI Text replacement
        document.querySelectorAll('[data-i18n]').forEach(el => {
            const key = el.getAttribute('data-i18n');
            if (t[key]) el.innerText = t[key];
        });

        // Link Href replacement
        const linkHome = document.getElementById('link-other-game');
        const linkOver = document.getElementById('link-other-game-over');
        if(linkHome) linkHome.href = t.linkDomain;
        if(linkOver) linkOver.href = t.linkDomain;

        // Font Adjustment for long titles
        const titleEl = document.getElementById('title-text');
        if (currentLang === 'ru' || currentLang === 'fr' || currentLang === 'es') {
            titleEl.style.fontSize = '16px'; // Shrink for longer languages
        } else {
            titleEl.style.fontSize = ''; // Reset
        }
    }

    // Initialize Language
    currentLang = detectLanguage();
    applyTranslations();

    /* -------------------------------------------------------------------------- */
    /* Game Logic                                  */
    /* -------------------------------------------------------------------------- */

    const canvas = document.getElementById('gameCanvas');
    const ctx = canvas.getContext('2d');
    const container = document.getElementById('app-container');
    const viewport = document.getElementById('game-viewport');
    const lcd = document.getElementById('lcd-screen');
    const scoreUI = document.getElementById('ui-score');
    const startUI = document.getElementById('start-screen');
    const gameOverUI = document.getElementById('game-over-screen');
    const finalScoreUI = document.getElementById('final-score');
    const panel = document.getElementById('control-panel');
    const itemMsgUI = document.getElementById('item-message');

    let gameActive = false;
    let score = 0;
    let energy = 12;
    let inputStr = "";
    let enemies = [];
    let sprinkles = [];
    let gameStartTime = 0;
    let lastSpawnTime = 0;
    let waveOffset = 0;
    let lastTime = 0; 
    let lastWaveTime = 0;
    const waveInterval = 500;

    let enemySpawnCount = 0;
    let nextItemTarget = rand(20, 30);
    let activeEffects = { hidden: false, speedUp: false, speedDown: false, frozen: false };
    let effectTimers = {}; 
    let itemMsgTimer = null;

    const AudioContext = window.AudioContext || window.webkitAudioContext;
    let audioCtx = new AudioContext();

    const SoundFX = {
        playTone: function(freq, type, duration) {
            if (audioCtx.state === 'suspended') audioCtx.resume();
            const osc = audioCtx.createOscillator();
            const gainNode = audioCtx.createGain();
            osc.type = type;
            osc.frequency.setValueAtTime(freq, audioCtx.currentTime);
            gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime);
            gainNode.gain.exponentialRampToValueAtTime(0.001, audioCtx.currentTime + duration);
            osc.connect(gainNode);
            gainNode.connect(audioCtx.destination);
            osc.start();
            osc.stop(audioCtx.currentTime + duration);
        },
        click: function() { this.playTone(800, 'square', 0.05); },
        correct: function() {
            if (audioCtx.state === 'suspended') audioCtx.resume();
            const now = audioCtx.currentTime;
            this.scheduleTone(523.25, 'square', now, 0.1); 
            this.scheduleTone(659.25, 'square', now + 0.1, 0.1); 
            this.scheduleTone(783.99, 'square', now + 0.2, 0.2); 
        },
        itemGet: function() {
            if (audioCtx.state === 'suspended') audioCtx.resume();
            const now = audioCtx.currentTime;
            this.scheduleTone(880, 'sine', now, 0.1);
            this.scheduleTone(1108, 'sine', now+0.1, 0.1);
            this.scheduleTone(1318, 'sine', now+0.2, 0.1);
            this.scheduleTone(1760, 'sine', now+0.3, 0.2);
        },
        damage: function() { this.playTone(150, 'sawtooth', 0.3); },
        start: function() {
            if (audioCtx.state === 'suspended') audioCtx.resume();
            const now = audioCtx.currentTime;
            this.scheduleTone(440, 'square', now, 0.1);
            this.scheduleTone(554, 'square', now + 0.1, 0.1);
            this.scheduleTone(659, 'square', now + 0.2, 0.3);
        },
        gameOver: function() {
            if (audioCtx.state === 'suspended') audioCtx.resume();
            const now = audioCtx.currentTime;
            this.scheduleTone(659, 'sawtooth', now, 0.2);
            this.scheduleTone(554, 'sawtooth', now + 0.2, 0.2);
            this.scheduleTone(440, 'sawtooth', now + 0.4, 0.4);
        },
        scheduleTone: function(freq, type, time, duration) {
            const osc = audioCtx.createOscillator();
            const gainNode = audioCtx.createGain();
            osc.type = type;
            osc.frequency.setValueAtTime(freq, time);
            gainNode.gain.setValueAtTime(0.1, time);
            gainNode.gain.exponentialRampToValueAtTime(0.001, time + duration);
            osc.connect(gainNode);
            gainNode.connect(audioCtx.destination);
            osc.start(time);
            osc.stop(time + duration);
        }
    };

    function drawAnalogClock() {
        const clockCanvas = document.getElementById('analog-clock');
        const cCtx = clockCanvas.getContext('2d');
        const w = clockCanvas.width;
        const h = clockCanvas.height;
        const cx = w / 2;
        const cy = h / 2;
        const radius = w / 2 - 2;

        const now = new Date();
        const min = now.getMinutes();
        const hour = now.getHours() % 12;
        const sec = now.getSeconds(); 

        cCtx.clearRect(0, 0, w, h);
        cCtx.fillStyle = "#ffe200"; 
        cCtx.fillRect(0, 0, w, h);

        cCtx.fillStyle = "#000";
        for (let i = 1; i <= 12; i++) {
            const angle = (i * 30) * Math.PI / 180;
            const dist = radius * 0.75;
            const dx = cx + Math.sin(angle) * dist;
            const dy = cy - Math.cos(angle) * dist;
            cCtx.beginPath();
            cCtx.arc(dx, dy, 1.5, 0, Math.PI*2); 
            cCtx.fill();
        }

        cCtx.save();
        cCtx.translate(cx, cy);
        cCtx.rotate(((hour + min/60) * 30) * Math.PI / 180);
        cCtx.strokeStyle = "#000"; 
        cCtx.lineWidth = 1; 
        cCtx.lineCap = "round";
        cCtx.beginPath();
        cCtx.moveTo(0, 0);
        cCtx.lineTo(0, -radius * 0.6); 
        cCtx.stroke();
        cCtx.restore();

        cCtx.save();
        cCtx.translate(cx, cy);
        cCtx.rotate(((min + sec/60) * 6) * Math.PI / 180);
        cCtx.strokeStyle = "#000"; 
        cCtx.lineWidth = 1;
        cCtx.lineCap = "round";
        cCtx.beginPath();
        cCtx.moveTo(0, 0);
        cCtx.lineTo(0, -radius * 0.9); 
        cCtx.stroke();
        cCtx.restore();
        
        cCtx.beginPath();
        cCtx.arc(cx, cy, 2, 0, Math.PI*2);
        cCtx.fillStyle = "#000";
        cCtx.fill();
    }
    
    setInterval(drawAnalogClock, 1000);
    drawAnalogClock();

    function resize() {
        const ww = window.innerWidth;
        const wh = window.innerHeight;
        const targetRatio = 4/3;
        
        let availableWidth = ww;
        let availableHeight = wh;
        let rotation = 0;

        if (wh > ww) {
            availableWidth = wh;
            availableHeight = ww;
            rotation = 90;
        }

        let newW, newH;
        if (availableWidth / availableHeight > targetRatio) {
            newH = availableHeight;
            newW = newH * targetRatio;
        } else {
            newW = availableWidth;
            newH = newW / targetRatio;
        }

        container.style.width = newW + 'px';
        container.style.height = newH + 'px';

        if (rotation === 90) {
            container.style.transform = `translate(-50%, -50%) rotate(90deg)`;
        } else {
            container.style.transform = `translate(-50%, -50%)`;
        }

        canvas.width = viewport.clientWidth;
        canvas.height = viewport.clientHeight;
    }
    window.addEventListener('resize', resize);

    function toggleFullScreen() {
        if (!document.fullscreenElement) {
            document.documentElement.requestFullscreen();
        } else {
            if (document.exitFullscreen) {
                document.exitFullscreen();
            }
        }
    }

    // 홈 버튼: 첫 화면으로 돌아가기
    function goToHome() {
        gameActive = false; // 게임 루프 중지
        startUI.style.display = 'flex'; // 시작 화면 보이기
        gameOverUI.style.display = 'none'; // 게임 오버 화면 숨기기
        itemMsgUI.style.opacity = '0'; // 아이템 메시지 숨기기
    }

    function initGame() {
        if (audioCtx.state === 'suspended') {
            audioCtx.resume();
        }
        startUI.style.display = 'none';
        gameOverUI.style.display = 'none';
        SoundFX.start();
        restartGame();
    }

    function restartGame() {
        score = 0;
        energy = 12;
        inputStr = "";
        enemies = [];
        sprinkles = [];
        gameStartTime = performance.now();
        lastTime = gameStartTime;
        gameActive = true;
        scoreUI.innerText = score;
        lcd.innerText = "";
        itemMsgUI.style.opacity = '0';
        
        enemySpawnCount = 0;
        nextItemTarget = rand(10, 20);
        
        activeEffects = { hidden: false, speedUp: false, speedDown: false, frozen: false };
        for (let key in effectTimers) clearTimeout(effectTimers[key]);
        effectTimers = {};
        
        document.querySelectorAll('.key').forEach(k => k.classList.remove('broken'));

        for(let i=0; i<3; i++) {
            spawnEnemy(true);
        }
        
        requestAnimationFrame(update);
    }

    function spawnEnemy(isInitial = false) {
        let attempts = 0;
        const maxAttempts = 50;
        
        let isItem = false;
        if (!isInitial) {
            enemySpawnCount++;
            if (enemySpawnCount >= nextItemTarget) {
                isItem = true;
                enemySpawnCount = 0;
                nextItemTarget = rand(10, 20);
            }
        }

        const centerX = canvas.width / 2;
        const safeMargin = 110; 
        const padding = 60; 

        while (attempts < maxAttempts) {
            let side = Math.random() > 0.5 ? 'left' : 'right';
            let x;

            if (side === 'left') {
                const leftWidth = (centerX - safeMargin) - padding;
                if (leftWidth > 0) {
                    x = Math.random() * leftWidth + padding;
                } else {
                    x = padding; 
                }
            } else {
                const rightStart = centerX + safeMargin;
                const rightWidth = (canvas.width - padding) - rightStart;
                if (rightWidth > 0) {
                    x = Math.random() * rightWidth + rightStart;
                } else {
                    x = canvas.width - padding;
                }
            }

            let rawY = isInitial ? Math.random() * (canvas.height * 0.3) + 60 : -40;
            let y = Math.floor(rawY / 40) * 40; 
            
            let overlap = false;
            for (let e of enemies) {
                if (Math.abs(e.x - x) < 140 && Math.abs(e.y - y) < 40) {
                    overlap = true;
                    break;
                }
            }
            if (!overlap) {
                enemies.push(new Enemy(x, y, isItem));
                return;
            }
            attempts++;
        }
    }

    function generateProblem(elapsed) {
        // [난이도별 문제 정의]
        const lv1 = ['add_s1', 'sub_s1', 'mul_s1']; // 3+5, 8-2, 3x4
        const lv2 = ['add_s2', 'sub_s2', 'mul_s2']; // 7+8, 15-6, 8x9
        const lv3 = ['add_2_1', 'sub_2_1', 'mul_tens', 'div_basic', 'add_2_2_easy', 'sub_2_2_easy']; 

        let selectedPool = [];

        // [단계별 출제 확률 로직 - 시간 여유를 둠]
        if (elapsed < 40) {
            // 1구간 (0~40초): 무조건 1단계 (몸풀기)
            selectedPool = lv1;
        } else if (elapsed < 80) {
            // 2구간 (40~80초): 1단계(50%) vs 2단계(50%)
            if (Math.random() < 0.5) selectedPool = lv1;
            else selectedPool = lv2;
        } else {
            // 3구간 (80초~): 1, 2, 3단계 골고루 (33%씩)
            const dice = Math.floor(Math.random() * 3);
            if (dice === 0) selectedPool = lv1;
            else if (dice === 1) selectedPool = lv2;
            else selectedPool = lv3;
        }

        const type = selectedPool[Math.floor(Math.random() * selectedPool.length)];
        let a, b, op, ans;

        switch(type) {
            // === 1단계 ===
            case 'add_s1': ans=rand(2,9); a=rand(1,ans-1); b=ans-a; op='+'; break;
            case 'sub_s1': a=rand(2,9); b=rand(1,a-1); op='-'; ans=a-b; break;
            case 'mul_s1': a=rand(2,5); b=rand(1,9); op='×'; ans=a*b; break;

            // === 2단계 ===
            case 'add_s2': a=rand(2,9); b=rand(10-a,9); op='+'; ans=a+b; break;
            case 'sub_s2': a=rand(11,18); b=rand(a-9,9); op='-'; ans=a-b; break;
            case 'mul_s2': a=rand(6,9); b=rand(2,9); op='×'; ans=a*b; break;

            // === 3단계 ===
            case 'add_2_1': a=rand(10,89); b=rand(1,9); op='+'; ans=a+b; break;
            case 'sub_2_1': a=rand(11,99); b=rand(1,9); op='-'; ans=a-b; break;
            case 'mul_tens': a=rand(2,9)*10; b=rand(2,9); op='×'; ans=a*b; break;
            case 'div_basic': b=rand(2,9); ans=rand(2,9); a=b*ans; op='÷'; break;
            case 'add_2_2_easy': a=rand(10,40); b=rand(10,40); op='+'; ans=a+b; break;
            case 'sub_2_2_easy': a=rand(30,80); b=rand(10,a-10); op='-'; ans=a-b; break;
        }

        return { text: `${a}${op}${b}`, answer: ans };
    }

    class Enemy {
        constructor(x, y, isItem = false) {
            this.x = x;
            this.y = y;
            this.stepSize = 40; 
            this.accumulatedTime = 0; 
            this.isItem = isItem;

            const elapsed = (performance.now() - gameStartTime) / 1000;
            const prob = generateProblem(elapsed);
            this.text = prob.text;
            this.answer = prob.answer;
            
            // 폰트 설정: 다국어 지원을 위해 폰트 스택 추가
            ctx.font = "normal 28px 'DosMyungjo', monospace, sans-serif"; 
            this.width = ctx.measureText(this.text).width;
            
            this.height = 30;

            // [속도 조절] 120초(2분)가 지나야 속도가 2배가 됨. 아주 천천히 빨라짐.
            const speedMultiplier = 1.0 + (elapsed / 120); 
            
            const baseInterval = 1000; 
            this.baseMoveInterval = baseInterval / speedMultiplier; 
            this.moveInterval = this.baseMoveInterval;
        }

        update(dt) {
            if (activeEffects.frozen) return;

            let currentInterval = this.baseMoveInterval;
            if (activeEffects.speedUp) currentInterval /= 1.3;
            if (activeEffects.speedDown) currentInterval /= 0.7;

            this.moveInterval = currentInterval;

            this.accumulatedTime += dt;
            if (this.accumulatedTime >= this.moveInterval) {
                this.y += this.stepSize;
                this.accumulatedTime = 0; 
            }
        }

        draw() {
            ctx.font = "normal 28px 'DosMyungjo', monospace, sans-serif";
            ctx.textAlign = "center";
            
            if (activeEffects.hidden) {
                ctx.fillStyle = "#808080"; 
            } else if (this.isItem) {
                ctx.fillStyle = "#ffe200";
            } else {
                ctx.fillStyle = "#fff";
            }
            
            if (this.isItem) {
                ctx.shadowColor = "#ffa500";
                ctx.shadowBlur = 5;
            } else {
                ctx.shadowColor = "rgba(0,0,0,0.5)";
                ctx.shadowBlur = 0;
            }

            ctx.fillText(this.text, this.x, this.y);
            
            ctx.shadowBlur = 0;
        }
    }

    function rand(min, max) { return Math.floor(Math.random() * (max - min + 1)) + min; }

    function keyInput(val) {
        if (!gameActive) return;
        SoundFX.click(); 
        
        if (val === 'C') {
            inputStr = "";
        } else if (val === 'E') {
            checkAnswer();
            inputStr = "";
        } else {
            if (inputStr.length < 5) inputStr += val;
        }
        lcd.innerText = inputStr;
    }

    function checkAnswer() {
        const userVal = parseInt(inputStr);
        let found = false;
        
        for (let i = 0; i < enemies.length; i++) {
            if (enemies[i].answer === userVal) {
                const target = enemies[i];
                score += 10;
                scoreUI.innerText = score;
                createVintageEffect(target.x, target.y, "success");
                enemies.splice(i, 1);
                
                if (target.isItem) {
                    SoundFX.itemGet();
                    applyRandomItem();
                } else {
                    SoundFX.correct();
                }
                
                found = true;
                break;
            }
        }
        
        if (!found && userVal) {
            SoundFX.playTone(100, 'sawtooth', 0.1); 
        }
    }

    function applyRandomItem() {
        // 아이템 이름을 현재 언어 설정에서 가져옴
        const t = translations[currentLang].items;
        
        const items = [
            { name: t.clearAll, type: "clearAll" },
            { name: t.virus, type: "virus" },
            { name: t.speedUp, type: "speedUp" },
            { name: t.speedDown, type: "speedDown" },
            { name: t.heal, type: "heal" },
            { name: t.freeze, type: "freeze" }
        ];
        
        const item = items[rand(0, items.length - 1)];
        showItemMessage(item.name);
        activateItemEffect(item.type);
    }

    function showItemMessage(name) {
        itemMsgUI.innerText = name;
        itemMsgUI.style.opacity = '1';
        
        if (itemMsgTimer) clearTimeout(itemMsgTimer);
        itemMsgTimer = setTimeout(() => {
            itemMsgUI.style.opacity = '0';
        }, 3000); 
    }

    function activateItemEffect(type) {
        switch(type) {
            case "clearAll":
                enemies.forEach(e => {
                    score += 10;
                    createVintageEffect(e.x, e.y, "success");
                });
                enemies = [];
                scoreUI.innerText = score;
                break;
                
            case "virus":
                if(effectTimers.hidden) clearTimeout(effectTimers.hidden);
                activeEffects.hidden = true;
                effectTimers.hidden = setTimeout(() => { activeEffects.hidden = false; }, 10000);
                break;
                
            case "speedUp":
                if(effectTimers.speedUp) clearTimeout(effectTimers.speedUp);
                activeEffects.speedUp = true;
                effectTimers.speedUp = setTimeout(() => { activeEffects.speedUp = false; }, 10000);
                break;
                
            case "speedDown":
                if(effectTimers.speedDown) clearTimeout(effectTimers.speedDown);
                activeEffects.speedDown = true;
                effectTimers.speedDown = setTimeout(() => { activeEffects.speedDown = false; }, 10000);
                break;
                
            case "heal":
                energy = 12;
                document.querySelectorAll('.key').forEach(k => k.classList.remove('broken'));
                break;
                
            case "freeze":
                if(effectTimers.frozen) clearTimeout(effectTimers.frozen);
                activeEffects.frozen = true;
                effectTimers.frozen = setTimeout(() => { activeEffects.frozen = false; }, 5000);
                break;
        }
    }

    function takeDamage(x, y) {
        if (energy > 0) {
            energy--;
            SoundFX.damage(); 
            const keyNodes = document.querySelectorAll('.key');
            const targetKey = keyNodes[energy];
            if (targetKey) targetKey.classList.add('broken');
            
            createVintageEffect(x, y, "fail");
            
            if (energy <= 0) endGame();
        }
    }

    function endGame() {
        gameActive = false;
        SoundFX.gameOver(); 
        finalScoreUI.innerText = score;
        gameOverUI.style.display = 'flex';
    }

    function createVintageEffect(x, y, type) {
        if (type === "success") {
            for(let i=0; i<10; i++) {
                sprinkles.push({
                    x, y,
                    vx: Math.cos(i) * 5,
                    vy: Math.sin(i) * 5,
                    life: 15,
                    color: "#fff",
                    size: 4
                });
            }
        } else {
            sprinkles.push({
                x, y, vx: 0, vy: 0, life: 8, color: "#f00", size: 50
            });
        }
    }

    function update(now) {
        if (!gameActive) return; 
        
        requestAnimationFrame(update); 

        const dt = now - lastTime;
        lastTime = now;

        const spawnDt = now - lastSpawnTime;
        const elapsed = (now - gameStartTime) / 1000;

        ctx.clearRect(0, 0, canvas.width, canvas.height);
        
        drawWater(now);

        // [물량 조절] 90초(1분 30초) 지나야 스폰 간격이 2배로 빨라짐
        const spawnMultiplier = 1.0 + (elapsed / 90);
        
        // 최소 0.3초(300ms) 간격까지 줄어듦 (처음엔 1.8초)
        let spawnInterval = Math.max(300, 1800 / spawnMultiplier);
        
        if (!activeEffects.frozen) {
            if (spawnDt > spawnInterval) {
                spawnEnemy();
                lastSpawnTime = now;
            }
        }

        const p = panel.getBoundingClientRect();
        const vRect = viewport.getBoundingClientRect();
        const pLeft = p.left - vRect.left;
        const pRight = p.right - vRect.left;
        const pTop = p.top - vRect.top;
        const pBottom = p.bottom - vRect.top;

        for (let i = enemies.length - 1; i >= 0; i--) {
            const e = enemies[i];
            e.update(dt); 
            e.draw();

            const enemyHalfW = e.width / 2;
            const enemyTop = e.y - 24; 
            const enemyBottom = e.y;    
            const enemyLeft = e.x - enemyHalfW;
            const enemyRight = e.x + enemyHalfW;

            const hitPanel = (
                enemyRight > pLeft &&
                enemyLeft < pRight &&
                enemyBottom > pTop &&
                enemyTop < pBottom
            );

            if (e.y > canvas.height - 40 || hitPanel) {
                takeDamage(e.x, e.y);
                enemies.splice(i, 1);
            }
        }

        for (let i = sprinkles.length - 1; i >= 0; i--) {
            const s = sprinkles[i];
            s.x += s.vx; s.y += s.vy; s.life--;
            ctx.fillStyle = s.color;
            ctx.fillRect(s.x - s.size/2, s.y - s.size/2, s.size, s.size);
            if (s.life <= 0) sprinkles.splice(i, 1);
        }
    }

    function drawWater(now) {
        if (now - lastWaveTime > waveInterval) {
            waveOffset += 0.5; 
            lastWaveTime = now;
        }

        ctx.fillStyle = "rgba(0, 70, 160, 0.6)";
        ctx.beginPath();
        const y = canvas.height - 40;
        ctx.moveTo(0, y);
        
        for (let x = 0; x <= canvas.width; x += 5) {
            ctx.lineTo(x, y + Math.sin(x * 0.1 + waveOffset) * 6);
        }
        ctx.lineTo(canvas.width, canvas.height);
        ctx.lineTo(0, canvas.height);
        ctx.fill();
    }

    window.addEventListener('keydown', (e) => {
        if (!gameActive) return;
        if (e.key >= '0' && e.key <= '9') keyInput(e.key);
        else if (e.key === 'Enter') keyInput('E');
        else if (e.key === 'Backspace' || e.key === 'Escape') keyInput('C');
    });

    setTimeout(() => {
        resize();
    }, 100);
</script>

</body>
</html>